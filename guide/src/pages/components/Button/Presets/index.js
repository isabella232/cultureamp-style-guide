const presets = {
  default: {
    react: require('./Default.js'),
    elm: require('./Default.elm').Elm.Default,
  },
  secondary: {
    react: require('./Default.js'),
    elm: require('./Default.elm').Elm.Default,
  },
};

export default presets;
