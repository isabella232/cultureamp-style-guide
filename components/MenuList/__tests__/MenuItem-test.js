jest.unmock('../MenuItem');

import React from 'react';
import shallowRender from 'ca-test-utils/shallowRender';
import * as ShallowTestUtils from 'react-shallow-testutils';
import { MenuItemAction } from '../MenuItem';
import styles from '../KebabMenu.scss';

describe('MenuItemAction', () => {
  let rendered;

  beforeEach(() => {});

  describe('when there is no icon');
  describe('when an icon is used');
  describe('when a hover icon is used');
  describe('when the action is a function');
  describe('when the action is a link');
});
